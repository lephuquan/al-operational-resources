# Task: Triển khai Order API (Spring Boot)

**ID:** 20250405-order-api  
**Trạng thái:** In Progress

## 1. Mục tiêu

Xây dựng API cho phép người dùng tạo đơn hàng, tính tổng tiền theo giỏ hàng và lưu vào PostgreSQL.

## 2. Context

- Bảng `orders` và `order_items` trong schema (xem `docs/architecture/database-design.md`).
- Gọi `ProductService` (hoặc port tương đương) để kiểm tra tồn kho trước khi tạo đơn.

## 3. Checklist cho AI

- [x] Tạo `OrderRequest` DTO + Bean Validation (`@NotNull`, `min = 1`, …).
- [ ] `OrderRepository` (Spring Data JPA).
- [ ] `OrderService.createOrder()` — transaction: lưu order + cập nhật tồn kho.
- [ ] Unit test (Mockito) cho service.
- [ ] `OrderController` expose endpoint.

## 4. Hướng dẫn đặc biệt

- Tham chiếu mã lỗi trong `docs/knowledge-base/error-codes.md` và map sang `ORDER_*` khi hết hàng.
- Dùng `@Transactional` cho luồng ghi nhiều bước.

## 5. Log

- **2026-04-05 10:00**: Đã xong DTO và validation — đang repository (ví dụ).
