# AGENTS (redirect)

The full personal AI workspace protocol lives in **`workspace/AGENTS.md`**.

Start there, then use **`al-run/current-task/`** for the active task contract and gate scripts under **`al-run/scripts/`**.
